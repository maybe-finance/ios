import SwiftUI

// This view is now replaced by ReadingTabView in AuthenticatedView.swift
struct ReadingView: View {
    var body: some View {
        Text("Reading View - Deprecated")
    }
}

#Preview {
    ReadingView()
}